import 'package:flutter/material.dart';

class PlantCard extends StatelessWidget {
  final Image image;
  final String title;
  final String price;
  final Image icon;

  PlantCard({Key key, this.image, this.title, this.price, this.icon})
      : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
